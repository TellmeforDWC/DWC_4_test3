<!--投稿エラーメッセージ-->
<%= render "error_message", book: @book %>
<!--投稿エラーメッセージ-->

<h3>Editing Book</h3>
<%= form_with model:@book, local:true do |f| %>
  <p>Title</p>
  <p><%= f.text_area :title %></p>
  <p>Opinion</p>
  <p><%= f.text_field :body %></p>
  <p><%= f.submit "Update Book" %></p>
<% end %>
<span>
  <%= link_to "Show", book_path(@book.id) %>
  ｜
  <%= link_to "Back", books_path %>
</span>